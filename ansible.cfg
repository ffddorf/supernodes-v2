[defaults]
inventory = netbox_inventory.yml
ansible_managed = Ansible managed!
forks = 100
pipelining = true
timeout = 15
roles_path = roles
remote_user = admin
host_key_checking = False

[ssh_connection]
scp_if_ssh = true
