[defaults]
inventory = netbox_inventory.yml
forks = 100
pipelining = true
timeout = 15
roles_path = roles
remote_user = admin
host_key_checking = False
interpreter_python = auto_silent

[ssh_connection]
scp_if_ssh = true
